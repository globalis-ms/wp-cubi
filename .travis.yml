dist: jammy

language: php

matrix:
  include:
    - php: 8.0

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

install:
    - composer install --no-interaction --prefer-dist

script:
    - ./vendor/bin/phpcs . -p --standard=./phpcs.xml
